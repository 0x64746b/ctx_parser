from setuptools import setup, find_packages
setup(
    name = "ctx_parser",
    version = "0.1",
    packages = find_packages(),

    # metadata for upload to PyPI
    author = "dtk",
    description = "A parser for `.ctx` files",
    keywords = "ctx aqbanking",
    url = "https://github.com/0x64746b/ctx_parser",
)
